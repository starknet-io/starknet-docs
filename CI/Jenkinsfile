pipeline {
    agent { label "gcp-hodor-slave-generic"}
    options {
       // Add timestamps to output.
       timestamps()
       timeout(time: 45, unit: 'MINUTES')
    }
    environment {
        TEST = "test"
    }
    stages {
        stage('Main Flow') {
            steps {
                echo "${TEST}"
            }
        }
    }
}