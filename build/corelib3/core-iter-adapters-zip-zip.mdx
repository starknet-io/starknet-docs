# zip

Converts the arguments to iterators and zips them.
See the documentation of `Iterator::zip` for more.
# Examples

```cairo
use std::iter::zip;

let xs = array![1, 2, 3];
let ys = array![4, 5, 6];

let mut iter = zip(xs, ys);

assert_eq!(iter.next().unwrap(), (1, 4));
assert_eq!(iter.next().unwrap(), (2, 5));
assert_eq!(iter.next().unwrap(), (3, 6));
assert!(iter.next().is_none());```

Fully qualified path: [core](./core.md)::[iter](./core-iter.md)::[adapters](./core-iter-adapters.md)::[zip](./core-iter-adapters-zip.md)::[zip](./core-iter-adapters-zip-zip.md)

<pre><code class="language-cairo">pub fn zip&lt;
    A,
    B,
    impl AIntoIter: IntoIterator&lt;A&gt;,
    impl BIntoIter: IntoIterator&lt;B&gt;,
    +Destruct&lt;AIntoIter::IntoIter&gt;,
    +Destruct&lt;B&gt;,
&gt;(
    a: A, b: B,
) -&gt; Zip&lt;IntoIter, IntoIter&gt;</code></pre>

