---
title: "core::dict::SquashedFelt252DictTrait"
---





## Signature

```rust
pub trait SquashedFelt252DictTrait
```



## Trait functions

### into_entries



Returns an array of `(key, first_value, last_value)` tuples.
The first value is always 0.
# Example

```rust
let squashed_dict = dict.squash();
let entries = squashed_dict.entries();
```





#### Signature

```rust
fn into_entries(self: SquashedFelt252Dict) -> Array
```


