---
title: "core::ops::range::RangeInclusiveTrait"
---





## Signature

```rust
pub trait RangeInclusiveTrait+PartialOrd>
```



## Trait functions

### contains



Returns `true` if `item` is contained in the range.

#### Signature

```rust
fn contains, +PartialOrd, T, +Destruct, +PartialOrd>(
    self: @RangeInclusive, item: @T,
) -> bool
```



#### Examples

```rust
assert!(!(3..=5).contains(@2));
assert!( (3..=5).contains(@3));
assert!( (3..=5).contains(@4));
assert!( (3..=5).contains(@5));
assert!(!(3..=5).contains(@6));

assert!( (3..=3).contains(@3));
assert!(!(3..=2).contains(@3));
```





### is_empty



Returns `true` if the range contains no items.

#### Signature

```rust
fn is_empty, +PartialOrd, T, +Destruct, +PartialOrd>(
    self: @RangeInclusive,
) -> bool
```




#### Examples

```rust
assert!(!(3_u8..=5_u8).is_empty());
assert!(!(3_u8..=3_u8).is_empty());
assert!( (3_u8..=2_u8).is_empty());
```




